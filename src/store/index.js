import Vue from 'vue';
import Vuex from 'vuex';
import dataList from './modules/dataList'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    dataList,
  },
});
